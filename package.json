{
  "name": "electron-ipc",
  "version": "1.0.0",
  "description": "Electron IPC example",
  "main": "build/main/main.js",
  "scripts": {
    "clean":"rm -rf build && mkdir build",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn clean && yarn build",
    "start": "node_modules/.bin/electron ./build/main/main.js"
  },
  "author": "Christoph Purrer",
  "license": "MIT",
  "devDependencies": {
    "electron": "^9.0.0",
    "typescript": "^3.8.3"
  }
}
